name: Apply Swing Trading website hierarchy

on:
  push:
    branches:
      - feature/website-swing-trading
    paths:
      - .github/workflows/apply-swing-trading.yml

permissions:
  contents: write

jobs:
  apply-and-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/website-swing-trading
          fetch-depth: 0

      - name: Refine Trading Systems category menu
        run: python .github/vixale-swing/refine_menu.py

      - name: Syntax check
        run: node --check app.js

      - name: Trading Systems hierarchy test
        run: node tests/test_trading_systems_hierarchy.js

      - name: Existing dashboard equity regression test
        run: node tests/test_dashboard_equity_curve.js

      - name: Whitespace check
        run: git diff --check -- app.js tests/test_trading_systems_hierarchy.js

      - name: Commit refinement
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.js tests/test_trading_systems_hierarchy.js
          git commit -m "Refine Trading Systems category menu"
          git push origin HEAD:feature/website-swing-trading
